name: Build and Deploy Payment Service

on:
  push:
    branches:
      - main
    paths:
      - "payment-service/**"

jobs:
  build-deploy:
    name: Build and Deploy Payment Service
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: 'corretto'
          cache: 'maven'

      - name: Unit Tests
        run: mvn -B test --file payment-service/pom.xml

      - name: Build the Application
        run: mvn -B clean package --file payment-service/pom.xml

      - name: Login to Docker Hub
        uses: docker/login-action@v6
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v3
        with:
          context: ./payment-service
          push: true
          file: ./payment-service/Dockerfile
          tags: |
            ${{ secrets.DOCKER_HUB_USERNAME }}/payment-service-ecommerce:${{ github.sha }}
            ${{ secrets.DOCKER_HUB_USERNAME }}/payment-service-ecommerce:latest
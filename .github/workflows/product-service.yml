name: Build and Deploy Product Service

on:
  push:
    branches:
      - main
    paths:
      - "product-service/**"

jobs:
  build-deploy:
    name: Build and Deploy Product Service
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: 'corretto'
          cache: 'maven'

      - name: Unit Test
        run: mvn -B test --file product-service/pom.xml

      - name: Build the application
        run: mvn -B clean package --file product-service/pom.xml -DskipTests

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v6
        with:
          context: ./product-service
          push: true
          file: ./product-service/Dockerfile
          tags: |
            ${{ secrets.DOCKER_HUB_USERNAME }}/product-service-ecommerce:${{ github.sha }}
            ${{ secrets.DOCKER_HUB_USERNAME }}/product-service-ecommerce:latest
